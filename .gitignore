# Dependencias: se reinstalan con `npm install`, nunca se versionan.
node_modules/

# Artefactos locales del sistema y del editor.
.DS_Store
Thumbs.db
*.log
npm-debug.log*

# Variables de entorno y secretos locales. La configuración del asistente de voz
# vive en el servidor, nunca en el repositorio — ver wj-admin/VOZ.md.
.env
.env.local
*.ini
elevenlabs.ini
wj-content/api/config.php

# OJO: dist/ SÍ se versiona a propósito — es lo que Plesk sirve.
