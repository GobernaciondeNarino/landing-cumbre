# Dependencias: se reinstalan con `npm install`, nunca se versionan.
node_modules/

# Artefactos locales del sistema y del editor.
.DS_Store
Thumbs.db
*.log
npm-debug.log*

# Variables de entorno y secretos locales. La clave de ElevenLabs no vive aquí
# sino en el servidor, fuera del document root — ver wj-admin/VOZ.md.
.env
.env.local
*.ini
elevenlabs.ini

# OJO: dist/ SÍ se versiona a propósito — es lo que Plesk sirve.
